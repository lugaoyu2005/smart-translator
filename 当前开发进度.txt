当前开发进度如下（基于本会话已确立的事实，更新于 2026-09-06）：

已交付功能 ✅
功能模块	状态	说明
在线翻译	✅ 完成	百度 + 有道，支持自动→中文，引擎切换；在线翻译引擎多选框（含小牛/DeepL/腾讯/阿里/自定义OpenAI兼容框架位）与截图菜单双向同步；"当前翻译源"设置与截图菜单选中项双向绑定
截图OCR	✅ 完成	Windows内置（默认）+ 有道OCR云接口（新增）；RapidOCR本地/Tesseract为"未接入"占位；ocr_stored_capture按设置分发
覆盖样式	✅ 完成	三选：黑底白字/白底黑字/无背景（overlay_mode），替换原颜色/透明度/自适应配置；严格对齐模式（overlay_expand）：译文区域=原文+10%避让重叠，字号最大化填充
语言对菜单	✅ 完成	截图菜单lang组件：原文按钮→译文按钮（共用容器），各弹原生样式语言菜单（自动检测+9语言）；Ctrl+Alt+B应用内反转；持久化default_translation_direction，切换后自动重译
报错显示	✅ 完成	错误信息在框选区域内水平垂直居中红字，选区过小改红色圆圈感叹号；菜单栏报错时保持可用，切引擎=全链路重试
窗口记忆	✅ 完成	主窗口尺寸记忆（上次大小/固定大小两种模式+自定义宽高），点×时保存，启动恢复
启动预热	✅ 完成	启动后截图窗口show→hide预热（前端暂停渲染无闪屏），消除首次触发截图卡顿
截图翻译全链路	✅ 完成	拖选→隐藏→截图→OCR→行分组→逐组翻译→覆盖层→按键组（引擎/复制/关闭/设置）
OCR 文字识别	✅ 完成	Windows 内置引擎，中文优先；小区域放大 + 词级行重组
单词预处理	✅ 完成	MiniMap / Mini_Map → Mini Map
引擎菜单 UI	✅ 完成	点击向上展开垂直引擎列表，点选重译
有道签名 bug	✅ 已修复	两轮修复：①超20字符截断规则 ②salt改纳秒级时间戳保证每请求唯一（207：同秒多段请求salt重复被判重放攻击，切换引擎必现，已实测修复）
主页弹性布局	✅ 完成	左 30% 菜单栏 + 右 70% 详细设置
系统托盘	✅ 完成	启动隐藏 + 点×隐藏到托盘 + 托盘菜单（显示主窗口/截图翻译/退出）+ 左键点托盘显示主窗口
开机自启动	✅ 完成	tauri-plugin-autostart 写注册表 HKCU\...\Run（已验证注册表生效），设置页开关实时生效
全局快捷键	✅ 完成	tauri-plugin-global-shortcut，Ctrl+Alt+S 截图 / Ctrl+Alt+T 主窗口+翻译测试页；按设置解析注册，保存后重注册，冲突/格式错误回显提示
术语库管理	✅ 完成	terms.json 持久化（与 settings.json 同目录）+ 增删术语/译法 + 优先级调整 + 使用50次自动升级落盘 + 术语管理页 UI（搜索/添加/删除/优先级）
工程基建	✅ 完成	git 仓库初始化（基线提交），.gitignore 排除 target/node_modules/settings.json（含密钥）；删除死代码 ScreenshotTranslation.tsx/.css；测试硬编码 API 密钥改为环境变量（BAIDU_APP_ID/BAIDU_SECRET/YOUDAO_APP_KEY/YOUDAO_APP_SECRET）
截图等待期反馈	✅ 完成	拆分截图/OCR：截图完成立即恢复UI，OCR/翻译期间显示加载面板（转圈+阶段文字+耗时+ESC取消），选区边框全程可见
覆盖块背景模式	✅ 完成	无背景模式（透明仅文字+白色光晕）+ 背景随文字自适应（贴合文字消除留白），设置页可开关
OCR符号纠错	✅ 完成	已删除剔除逻辑（不再自动删字符）；改为双层映射纠正：内置穷举表（箭头形近字乛⺂⺡乁→→←、竖线丨→|、省略号⋯→…、带圈数字①-⑳→1-20、括号数字⑴-⑽、全角＠）+ 术语管理用户自定义映射（1-2字非字母数字源词）；未映射符号原样透传
引擎同步	✅ 修复	设置保存先落盘再重载引擎（原并发竞态导致截图窗口引擎列表不同步）
截图交互	✅ 完成	块外左键单击=隐藏/显示覆盖层；块内拖选=原生选文字；双击块=选中该段（Ctrl+双击多选，蓝色描边）；复制优先级=拖选文字>选中段落>全部（原/译模式）
Ctrl多段拖选	✅ 完成	按住Ctrl拖动（块内外）累积文字片段，顶部徽标计数；仅Ctrl时光标变箭头；双击防闪（抑制原生选词）；复制按操作顺序合并
拖选/双击统一	✅ 完成	双击块=原生选区（实心蓝）选中整段文字；Ctrl+双击=整段加入片段累积；无行为开关；复制优先级=原生选区>Ctrl片段>全部；无顶部提示栏
覆盖层字号自适应	✅ 完成	canvas 实测 + 二分查找，消除文字溢出；字号下限 8px→10px
水平间隔拆分	✅ 完成	同行左右间隔>1.5×行高拆为独立块（表格两列/标签+值）；前端分组增加同行x不相交强制拆分；被剔除杂符词保留为占位盒桥接间隙（修复连续长文本被误拆两段）

自动化验证 ✅
- npm run build（tsc + vite）通过
- cargo test：13 通过 / 0 失败 / 1 忽略（真实 API 测试需环境变量）
- cargo check 无错误（仅 3 个历史占位代码 dead_code 警告）
- dev 启动日志无 [启动] 错误（快捷键注册、自启动应用均成功）
- 启动隐藏已实测（启动后无主窗口）；注册表已实测写入

待亲手验证（dev 实例运行中）🧪
- 托盘右键菜单：显示主窗口 / 截图翻译 / 退出
- Ctrl+Alt+S 触发截图翻译；Ctrl+Alt+T 显示主窗口并跳转翻译测试页
- 主窗口点 × 隐藏到托盘（不退出）
- 术语管理页：添加/搜索/优先级/删除，重启后仍在（terms.json）

规划已定稿、待实施 📋
功能模块	状态	说明
（无）——原四项已全部完成

调研完成、待实施 🔍
功能模块	状态	说明
离线翻译库	🔍 选型确定	CTranslate2 + Marian，int8 单方向约 80–120MB
Win11 右键菜单	🔍 方案确定	single-instance + 注册表 shell 菜单 + 读选中文本
划词翻译 UI	🔍 待实施	设置页"划词翻译"菜单当前为占位（可与右键菜单同期做）

依赖中 / 未排期 ⏳
功能模块	状态	说明
网络动态切换引擎	⏳ 依赖离线库	在线用 API、离线用本地模型，待离线引擎落地后接

备注 ⚠️
- Ctrl+Alt+T 当前语义=调出主窗口到翻译测试页；划词翻译实现后可升级为划词翻译
- settings.json / terms.json 存 exe 同目录（便携式设计）；未来安装版需迁移到 app_data_dir
- 真实 API 密钥现仅存于 target/debug/settings.json（已被 gitignore，不入库）
弹层统一样式	✅ 完成	引擎/原文/译文弹层：向上展开、以按钮水平居中，尺寸=触发按钮宽×3高×3（视口钳制），超出内部滚动
菜单顺序修复	✅ 完成	截图菜单按components数组顺序渲染（原按JSX写死顺序导致排序不生效）；设置页按保存顺序显示
保存弹窗	✅ 完成	保存成功改GUI提示（遮罩点击关闭，仅居中标题"设置已保存"）；失败仍alert
官网链接	✅ 完成	百度/有道API标题旁大号→按钮，默认浏览器打开官网（open_external命令，仅http/https）
供应商翻译实现	✅ 完成	小牛翻译（REST，注册有免费额度）、DeepL（Free版每月50万字符，fx密钥自动区分域名）、自定义OpenAI兼容（Base URL+Key+模型名，可接DeepSeek/Kimi/Ollama等）；TranslationManager重构为通用引擎列表，与在线引擎多选框/截图菜单打通
RapidOCR本地OCR	✅ 完成	PaddleOCR PP-OCRv6 模型+ONNX Runtime推理，完全离线免费无限量；首次使用自动下载模型(约15MB)到 models/；关方向分类提速；设置页已转"已接入"
待接入供应商	📋 框架就绪	腾讯云(TC3签名)/阿里云(RPC签名)/火山引擎——签名复杂，框架和配置已就绪，翻译实现待后续
供应商翻译实现	✅ 完成	已接入7家：百度、有道、腾讯云（TC3签名，月免费500万字符）、阿里云（RPC签名，月免费约100万字符）、小牛、DeepL（Free版月50万字符）、自定义OpenAI兼容；全部与多选框/截图菜单/当前翻译源打通
划词翻译	✅ 完成	全局热键Ctrl+Alt+X（可配置/可禁用）：任意应用选中文本→模拟Ctrl+C捕获（剪贴板自动还原）→主窗口翻译页自动翻译；限制：禁用复制功能的应用不支持
单实例	✅ 完成	tauri-plugin-single-instance：二次启动唤起已有主窗口
右键菜单	⚠️ 系统限制	Windows无法为任意应用的选中文本添加右键菜单项（注册表仅支持文件/文件夹），以全局快捷键方案替代；如需文件右键翻译可后续加注册表项

## 2026-09-06 有道OCR云接口调通（第七轮收尾）
- 有道通用OCR（ocrapi）正式接入：抓官方文档修正三处关键参数——签名规则改为 img前10字符+img长度+img后10字符 截断拼接（非 sha256(q)）；参数名 imageType/docType/detectType=10012（按行识别）；langType 用 "auto"
- 响应解析按官方结构：Result.regions[].lines[].{text, boundingBox}（逗号分隔8值四角坐标 → 包围盒 → OcrLineInfo）
- GDI渲染"Hello World 123"实测云端识别通过；全量 cargo test 18 通过；npm run build 通过
- 至此 OCR 引擎三选齐备：Windows内置 / RapidOCR本地（PP-OCRv6离线免费）/ 有道OCR云端

## 2026-09-06 全库代码审查与修复（第八轮）
- 修复回归：截图菜单组改为按 settings.screenshot_components 数组顺序渲染（设置页上移/下移此前不生效）
- 修复：ESC退出/重新触发时作废进行中的截图流水线（runId守卫），不再在流程完成后把窗口带报错/旧结果弹回
- 修复：复制优先级——Ctrl累积片段优先于残留原生选区（此前Ctrl多选后复制只得到最后一段）；普通双击与普通拖动对齐为覆盖式单选（清空累积）
- 清理：删除死代码 collect_points/TranslationRequest；GDI清理调用显式处理返回值（编译警告清零）；history锁中毒时into_inner恢复
- 全量 cargo test 18 通过；npm run build 通过

## 2026-09-06 离线翻译引擎落地（第九轮）
- 技术栈：ort 2.0-rc.13（与 RapidOCR 复用同一 ONNX Runtime）+ HuggingFace tokenizers 纯Rust，零C++工具链依赖
- 模型：Helsinki-NLP OPUS-MT int8量化（Xenova ONNX镜像），中↔英专门模型直达；日/韩/俄/法/德/西/葡经英语中转（两次解码）
- 分发：按需自动下载（hf-mirror.com优先、huggingface.co兜底），每语言对约30-80MB，存exe同目录models/mt/；下载进度经offline-mt-status事件推送
- 新模块 offline_mt.rs：模型目录catalog、多源下载器（断点目录+8MB节流进度）、tokenizer/双会话懒加载缓存、贪心解码（no_repeat_ngram=3 + repetition_penalty=1.2 抑制复读循环）、脚本启发式auto检测（谚文→韩/假名→日/汉字→中/西里尔→俄/拉丁→英）、pivot路由
- 接入：OfflineMtEngine 实现TranslationEngine（名称"离线翻译"）append在引擎列表末尾——在线全失败自动兜底，截图菜单/当前翻译源可手动选中；翻译历史自动记录
- 设置：复用offline_engine字段（marian=启用[默认]/disabled=禁用），migrate把argos归一为marian；设置页下拉改造+说明；当前翻译源下拉加入"离线翻译（本地模型）"
- 修复既有不一致：migrate的current_engine校验只认百度/有道两个名（腾讯/阿里/小牛/DeepL/自定义会被误回落）→ 全量7引擎名映射；前端自定义引擎label与后端名不一致（自定义（OpenAI兼容）vs 自定义AI）→ 存储统一用后端名（ONLINE_PROVIDERS加engine字段）
- 修Xenova tokenizer.json兼容：precompiled_charsmap为null时剥掉Precompiled normalizer再加载
- 端到端实测通过：en→zh / zh→en / ja→zh(pivot) 三方向真实下载+推理（模型缓存后单段约1-3秒）
- 已知局限（后续项）：en→zh输出偏繁体（OPUS-MT语料倾向，可加简繁转换表）；贪心解码偶有"park park"级复读（beam search可根治）；截图翻译多段文本逐段解码偏慢
- 全量 cargo test 20 通过；npm run build 通过

## 2026-09-06 离线翻译质量三件套（第十轮，搁置项继续）
- 繁→简转换：en→zh 输出统一转简体，用 Windows 系统级 LCMapString（LCMAP_SIMPLIFIED_CHINESE，全字表零维护），目标语言为中文时应用；单测覆盖
- Beam search：束宽4、批量化前向（每步一次 [B,t] 解码，速度损失小）、EOS完成池+GNMT长度惩罚 ((5+len)/6) 评分——替换贪心解码
- 三重重复抑制：no_repeat_ngram=2（防长程循环）+ repetition_penalty=1.2（防复读倾向）+ 紧邻重复A A重罚-4.0（软禁"park park"式复读，保留"谢谢"类叠词可能）
- 实测对比：en→zh"這是一個測試"→"这是一个测试"；zh→en"park park"复读消除；ja→zh从数百字死循环→可读大意输出
- 全量 cargo test 21 通过；npm run build 通过

## 2026-09-06 离线翻译 KV cache（第十一轮，搁置项继续）
- 解码器切换为 decoder_model_merged_quantized.onnx（If双分支图）：首步 use_cache_branch=false，后续步仅喂各束最新token + past缓存——消除每步全量重解码
- 关键发现：merged 导出的 present 输出含 decoder.*（自注意力，正确增长）与 encoder.*（cross-attention KV）两组，且 **true 分支的 encoder present 是空张量（batch=0）**——处理为两组分离：decoder 组每步更新+按束重排；encoder 组仅首步取值，之后恒定回喂（cross KV 不随步变化）、只随 beam 父束复制
- past 布局实测 [batch, heads, seq, head_dim]（元数据 [-1,8,1,64]，seq 动态维声明为字面1）；首步零长度 past 按元数据构造（seq=0）
- beam 重排：每步选择后按父束索引 gather 各层 KV 行（Row主序按 batch 维切分复制）
- 实测：三方向正确性保持；首次翻译 4.2s（含会话加载）、纯推理 1.9s/短句——短句场景与全量重解码相当（If 图开销抵消部分收益），长句/多段场景受益显著；会话加载为进程内一次性成本
- 下载清单更新：新语言对下载 merged 版（无缓存版不再下载，已缓存对自动补下 merged）
- 全量 cargo test 21 通过

## 2026-09-08 实测反馈修复（第十二轮）+ release 打包
- release 打包：绿色版 smart-translator.exe（44MB）+ MSI 安装包；用户配置（settings/terms/history）与已下载离线翻译模型迁移到 release 目录
- 修复：保存设置按钮未贴底——sticky bottom:-30px 的负值 hack 把按钮条推出了可视区（sticky 的 bottom 本就相对滚动可视区），改 bottom:0 并补 padding-bottom
- 保存成功提示 2 秒后自动消失（此前仅点击遮罩关闭）
- 已重打 release exe 交付

## 2026-09-08 实测反馈第二轮修复（第十三轮）
- 离线翻译同语言（如中文→中文）误报“暂不支持”：改为直接原文返回
- 保存栏两项修正：内容不满一屏时沉底（panel flex column + min-height:100% + margin-top:auto，等价用户建议的 layout_weight）；滚动穿模观感加 box-shadow 分层
- 开机启动“无效”根因：启动后按设计隐藏到托盘，用户以为没启动——现在自启动注册表带 --autostart 参数，开机拉起时显示主窗口；注册表值规范化（带引号+参数）
- 翻译引擎徽标修正：统一动态显示“已接入/已接入·待填密钥”（按密钥是否填写），百度/有道不再缺失徽标；自定义引擎设置页显示名统一为“自定义AI（OpenAI兼容）”；hint 更新为含离线翻译
- 官网→按钮改镂空描边+圆角正方形（8px），hover 反色
- 全量 cargo test 21 通过；release exe 已重打包

## 2026-09-08 反馈修正续（第十四轮）
- 回退“自启动显示主窗口”：用户确认不需要（此前只是没找到托盘图标）；注册表值恢复为纯路径
- 7 个预装 API 密钥区常显（不再随启用开关隐藏）；小牛/DeepL/腾讯云/阿里云标题误标“未接入”修正（早已全部接入）并补全→官网按钮（小牛/DeepL/腾讯云TMT/阿里云MT）；自定义AI API 标题同步
- 带→按钮的标题 label 加 flex 垂直居中（.form-label.with-link）
- 全量 cargo test 21 通过；release exe 已重打包

## 2026-09-08 搁置项处理（第十五轮）——Android 已确认搁置
- 常驻透明+点击穿透架构落地：截图窗口启动即全屏透明+鼠标穿透常驻（TOOLWINDOW 不进 Alt+Tab），触发截图仅取消穿透——十字准星零延迟出现，取代旧 1.5s show/hide 预热方案；主窗口“开始截图”按钮同步适配；退出（ESC/右键/复制完成）回到穿透待命态，窗口不再隐藏
- 严格对齐字距/行距自适应：fitFontSize 升级——标准行距放不下时压行距(1.12)换更大字号；多行且高度富余时行距上浮(≤1.45)铺满色块；返回 {size,lineHeight} 供渲染
- 死代码清理：ScreenshotRegion / calculate_extract_button_position 及其测试删除，cargo 警告清零
- 全量 cargo test 19 通过；release exe 已重打包

## 2026-09-08 常驻架构焦点修复（第十六轮）
- 实测反馈：常驻架构下触发截图后 ESC/左右键全部无响应（穿透取消但窗口拿不到系统激活——已可见窗口 show() 是空操作）
- 修复：触发时 hide→取消穿透→show→set_focus 重新激活（合成已预热，显隐开销极小）；主窗口“开始截图”按钮同步
- cargo test 19 通过；release exe 已重打包

## 2026-09-08 启动待命态（第十七轮）
- 实测反馈：常驻窗口启动即渲染框选暗幕提示（透明窗口内容可见），用户以为“一打开就是截图翻译”
- 修复：新增 idle 待命态——启动后窗口常驻但完全透明无任何内容；仅快捷键/主窗口按钮触发（trigger-screenshot→resetState→select）才显示截图界面；退出（ESC/右键/复制）回 idle
- release exe 已重打包

## 2026-09-08 截图触发双通道修复（第十八轮）
- 主窗口“开始截图”按钮失败根因：只做了窗口显隐，idle 架构下前端需 trigger-screenshot 事件才进入框选——新增 trigger_screenshot_cmd，按钮与快捷键/托盘走同一后端路径
- ESC 失效根因：窗口偶发拿不到键盘焦点（Windows 前台锁定），keydown 收不到——新增全局 ESC 热键兜底（trigger 时注册、result 阶段/退出时注销，热键吞键窗口期仅限框选+处理的几秒内）
- 状态机自查（回归+负向）：idle穿透无响应✓/processing中ESC作废流水线✓/processing中再触发重置✓/exit幂等✓/热键残留防护（is_registered去重）✓
- cargo test 19 通过；release exe 已重打包

## 2026-09-08 常驻架构回退为“预热+隐藏待命”（第十九轮）
- 实测反馈：右键退出后暗幕消失但鼠标仍十字准星、任何点击无反馈——运行时 setIgnoreCursorEvents(true) 在可见窗口上未生效（穿透无法恢复），窗口残留拦截所有输入
- 决策：放弃“窗口常驻穿透”模型，回退为“预热+隐藏待命”——启动时置穿透显示一次（idle零渲染无感知，完成合成初始化）后隐藏；此后触发/退出均为显隐切换；退出强制 win.hide() 兜底（隐藏窗口不可能拦截任何输入，穿透恢复失败也有兜底）；TOOLWINDOW 保留
- 穿透切换统一在隐藏态进行（行为一致对称）；触发路径不变（hide→取消穿透→show→focus→事件）
- release exe 已重打包

## 2026-09-08 截图焦点链路深度修复（第二十轮，自动化验证通过）
- 深度测试定位：快捷键触发后窗口显示但从未成为前台（Windows 拒绝后台进程 SetForegroundWindow）——键盘输入全给别的窗口，ESC/按键全失效；ESC 热键兜底 on_shortcut("Escape") 也静默注册失败
- 修复一：ALT 击键 hack——keybd_event(VK_MENU) 使本进程获得前台设置权后 SetForegroundWindow+SetWindowPos TOPMOST，实测触发后前台归属正确
- 修复二：弃用热键兜底，改为 GetAsyncKeyState 轮询（poll_esc 命令，150ms，读全局物理键状态与焦点无关）；idle 态不轮询
- 自动化测试（SendKeys+Win32窗口/前台检查）全链路通过：触发显示+焦点到手 / ESC退出隐藏+焦点释放 / 重复触发回归 / 清理
- cargo test 19 通过；release exe 已重打包并重启
