# 智能翻译软件开发计划

## 一、项目概述
基于用户需求，开发一款支持Windows 11及未来安卓平台的智能翻译软件，集成离线翻译、在线翻译、截图翻译、划词翻译等功能。

## 二、技术架构

### 1. 技术栈选择
- **前端框架**: React + TypeScript + Tailwind CSS
- **后端框架**: Rust + Tauri 2.0
- **离线翻译引擎**: Marian/NMT（微软翻译开源版）
- **在线翻译API**: 百度翻译 + 有道智云（免费额度）
- **OCR引擎**: Tesseract OCR（开源跨平台）
- **构建工具**: Vite + Cargo

### 2. 系统架构图
```
┌─────────────────────────────────────────────────────┐
│                    用户界面层 (React)                  │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐ │
│  │ 设置页面 │  │截图翻译UI│  │术语管理 │  │ 主界面  │ │
│  └─────────┘  └─────────┘  └─────────┘  └─────────┘ │
└──────────────────────┬──────────────────────────────┘
                       │ Tauri IPC
┌──────────────────────┴──────────────────────────────┐
│                  业务逻辑层 (Rust)                    │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐ │
│  │翻译引擎 │  │ OCR处理 │  │系统集成 │  │术语管理 │ │
│  │  管理   │  │  模块   │  │  模块   │  │  引擎   │ │
│  └─────────┘  └─────────┘  └─────────┘  └─────────┘ │
└──────────────────────┬──────────────────────────────┘
                       │
┌──────────────────────┴──────────────────────────────┐
│                  平台抽象层 (Rust)                    │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐ │
│  │Windows  │  │ Android │  │  网络   │  │ 文件   │ │
│  │  API    │  │   API   │  │  状态   │  │  存储   │ │
│  └─────────┘  └─────────┘  └─────────┘  └─────────┘ │
└─────────────────────────────────────────────────────┘
```

## 三、功能模块详细设计

### 1. 翻译引擎管理模块
**功能**:
- 离线翻译：集成Marian/NMT引擎
- 在线翻译：百度翻译API + 有道智云API
- 网络状态检测：根据网络自动切换翻译方式
- 术语优先级管理：自动调整 + 手动微调

**技术实现**:
```rust
// Rust后端翻译引擎接口
pub trait TranslationEngine {
    async fn translate(&self, text: &str, from: &str, to: &str) -> Result<String>;
    fn is_available(&self) -> bool;
    fn get_engine_type(&self) -> EngineType;
}

// 翻译引擎管理器
pub struct TranslationManager {
    engines: Vec<Box<dyn TranslationEngine>>,
    term_base: TermBase,
    network_monitor: NetworkMonitor,
}
```

### 2. 截图翻译模块
**功能**:
- 屏幕截图捕获
- OCR文字识别（Tesseract）
- 翻译结果显示（覆盖原文位置）
- 交互按钮：提取按钮（原始文字/翻译结果）
- 左键隐藏/显示，右键退出

**技术实现**:
```rust
// 截图翻译核心结构
pub struct ScreenshotTranslator {
    ocr_engine: TesseractOCR,
    translation_manager: TranslationManager,
    ui_renderer: UIRenderer,
}

// 提取按钮位置计算
fn calculate_extract_button_position(
    selection_rect: &Rect,
    screen_bounds: &Rect
) -> Point {
    // 根据需求：下方边框居中下方对齐
    // 超出屏幕时改为上方对齐
    // 不透明度10%，悬停时100%
}
```

### 3. 划词翻译模块
**功能**:
- 鼠标选中文本检测
- Windows 11右键菜单集成
- 快捷键触发翻译
- 文本预处理：处理MiniMap、Mini_Map等格式

**技术实现**:
```rust
// 文本预处理函数
fn preprocess_text(text: &str) -> String {
    // 1. 处理首字母大写无间隔：MiniMap -> Mini Map
    // 2. 处理下划线格式：Mini_Map -> Mini Map
    // 3. 其他特殊格式处理
    let re_camel = Regex::new(r"([a-z])([A-Z])").unwrap();
    let result = re_camel.replace_all(text, "$1 $2");
    
    let re_underline = Regex::new(r"_").unwrap();
    re_underline.replace_all(&result, " ").to_string()
}

// Windows右键菜单注册
fn register_context_menu() -> Result<()> {
    // 注册到Windows 11右键菜单
    // 需要处理Windows注册表或使用Windows API
}
```

### 4. 系统集成模块
**功能**:
- 开机自启动
- 系统托盘最小化
- 快捷键全局监听
- 网络状态监控

**技术实现**:
```rust
// 系统托盘管理
pub struct SystemTray {
    tray_icon: TrayIcon,
    menu: Menu,
}

// 开机自启动注册
fn setup_autostart() -> Result<()> {
    // Windows: 写入注册表HKCU\Software\Microsoft\Windows\CurrentVersion\Run
    // Android: 使用BootCompleted receiver
}

// 全局快捷键监听
fn setup_global_hotkeys() -> Result<()> {
    // 注册全局快捷键，如Ctrl+Alt+T触发翻译
}
```

### 5. 用户界面模块
**功能**:
- 弹性盒子设置页面（30%菜单 + 70%详细设置）
- 术语管理界面
- 翻译历史记录
- 配置导入导出

**React组件结构**:
```tsx
// 主应用组件
function App() {
  return (
    <div className="flex h-screen">
      <Sidebar className="w-[30%] bg-gray-100" />
      <main className="w-[70%] p-6">
        <SettingsPanel />
      </main>
    </div>
  );
}

// 截图翻译浮动UI
function ScreenshotTranslationUI() {
  return (
    <div className="fixed inset-0 z-50">
      <SelectionOverlay />
      <ExtractButton 
        className="opacity-10 hover:opacity-100"
        draggable={true}
      />
      <TranslationResult />
    </div>
  );
}
```

## 四、开发阶段规划

### 第一阶段：基础框架搭建（2-3周）
1. 初始化Tauri项目结构
2. 集成React前端框架
3. 实现基础窗口管理和系统托盘
4. 搭建翻译引擎基础接口

### 第二阶段：核心翻译功能（3-4周）
1. 集成Marian/NMT离线引擎
2. 实现百度翻译和有道智云API
3. 开发网络状态检测和自动切换
4. 实现文本预处理功能

### 第三阶段：截图翻译功能（2-3周）
1. 集成Tesseract OCR引擎
2. 实现屏幕截图捕获
3. 开发截图翻译UI和交互逻辑
4. 实现提取按钮和位置计算

### 第四阶段：系统集成功能（1-2周）
1. 实现开机自启动
2. 开发右键菜单集成
3. 实现全局快捷键
4. 优化系统资源占用

### 第五阶段：界面和体验优化（2-3周）
1. 完善设置页面布局
2. 开发术语管理界面
3. 实现配置导入导出
4. 进行用户体验优化

### 第六阶段：测试和发布（1-2周）
1. 功能测试和bug修复
2. 性能优化
3. 编写用户文档
4. 准备发布包

## 五、技术难点与解决方案

### 1. Marian/NMT集成
**难点**: Marian/NMT是C++库，需要与Rust集成
**解决方案**: 
- 使用`cxx` crate进行C++/Rust互操作
- 预编译Marian/NMT为动态链接库
- 设计异步接口避免阻塞UI

### 2. 跨平台OCR
**难点**: Tesseract在不同平台需要不同配置
**解决方案**:
- 为Windows和Android分别编译Tesseract
- 使用条件编译处理平台差异
- 预打包中文训练数据

### 3. Windows 11右键菜单
**难点**: Windows 11对右键菜单有新限制
**解决方案**:
- 研究Windows 11右键菜单扩展机制
- 可能需要使用Windows App SDK
- 提供备选方案（全局快捷键）

### 4. 截图翻译性能
**难点**: 实时截图和OCR可能影响性能
**解决方案**:
- 使用GPU加速截图捕获
- 异步处理OCR和翻译
- 实现智能缓存机制

## 六、依赖库列表

### Rust后端依赖
```toml
[dependencies]
tauri = { version = "2.0", features = ["shell-open"] }
serde = { version = "1.0", features = ["derive"] }
tokio = { version = "1.0", features = ["full"] }
reqwest = { version = "0.11", features = ["json"] }
opencv = "0.82"  # 用于图像处理
tesseract = "0.10"  # OCR
windows = { version = "0.52", features = ["Win32_System_Registry"] }
```

### 前端依赖
```json
{
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "@tauri-apps/api": "^2.0.0",
    "tailwindcss": "^3.3.0",
    "zustand": "^4.4.0"
  }
}
```

## 七、风险评估与应对

### 1. 技术风险
- **Marian/NMT集成复杂**: 准备备选离线引擎（Argos Translate）
- **跨平台兼容性**: 早期进行多平台测试
- **性能问题**: 实现性能监控和优化机制

### 2. 资源风险
- **开发时间**: 采用敏捷开发，分阶段交付
- **模型文件大小**: 提供模型按需下载功能
- **API限制**: 实现请求缓存和限制机制

### 3. 用户体验风险
- **功能复杂**: 提供简洁的默认配置
- **学习成本**: 设计直观的交互流程
- **系统资源占用**: 实现资源使用优化

## 八、下一步行动

1. **技术验证**: 创建Tauri项目原型，验证关键技术点
2. **环境搭建**: 配置开发环境，包括Rust、Node.js、Tauri CLI
3. **界面设计**: 设计详细的UI/UX原型
4. **开发启动**: 按照阶段计划开始第一阶段开发

---
*文档创建时间: 2024年12月19日*
*版本: v1.0*