# 智能翻译软件开发环境设置指南

## 一、必需工具安装

### 1. Rust安装
**下载地址**: https://rustup.rs/

**安装步骤**:
1. 访问 https://rustup.rs/
2. 下载 `rustup-init.exe`
3. 运行安装程序，选择默认安装
4. 安装完成后，重启命令行工具

**验证安装**:
```bash
rustc --version
cargo --version
```

**注意**: Rust安装需要Visual Studio Build Tools。如果遇到错误，请先安装Visual Studio Build Tools。

### 2. Node.js安装
**下载地址**: https://nodejs.org/

**安装步骤**:
1. 访问 https://nodejs.org/
2. 下载LTS版本（推荐v18+）
3. 运行安装程序，选择默认安装
4. 安装完成后，重启命令行工具

**验证安装**:
```bash
node --version
npm --version
```

### 3. Tauri CLI安装
**安装命令**:
```bash
npm install -g @tauri-apps/cli
```

**验证安装**:
```bash
tauri --version
```

## 二、开发工具安装

### 1. VS Code安装
**下载地址**: https://code.visualstudio.com/

**推荐扩展**:
- **Tauri**: Tauri开发支持
- **rust-analyzer**: Rust语言支持
- **ES7+ React/Redux/React-Native snippets**: React代码片段
- **Prettier**: 代码格式化
- **ESLint**: JavaScript代码检查

### 2. Git安装
**下载地址**: https://git-scm.com/

**安装步骤**:
1. 访问 https://git-scm.com/
2. 下载Windows版本
3. 运行安装程序，选择默认安装

**验证安装**:
```bash
git --version
```

## 三、项目初始化

### 1. 创建项目目录
```bash
mkdir smart-translator
cd smart-translator
```

### 2. 初始化Tauri项目
```bash
tauri init
```

**配置选项**:
- **窗口标题**: 智能翻译软件
- **前端开发服务器**: http://localhost:5173
- **前端构建目录**: ../dist
- **前端构建命令**: npm run build
- **前端开发命令**: npm run dev

### 3. 安装前端依赖
```bash
npm install
```

### 4. 安装Rust依赖
```bash
cd src-tauri
cargo build
```

## 四、开发环境验证

### 1. 启动开发服务器
**终端1** - 启动前端开发服务器:
```bash
npm run dev
```

**终端2** - 启动Tauri开发模式:
```bash
tauri dev
```

### 2. 验证功能
1. 检查窗口是否正常显示
2. 测试基本交互功能
3. 验证前后端通信

## 五、常见问题解决

### 1. Rust安装失败
**问题**: 安装过程中遇到错误
**解决方案**:
1. 安装Visual Studio Build Tools
2. 选择"使用C++的桌面开发"工作负载
3. 重启计算机后重新安装Rust

### 2. Tauri构建失败
**问题**: `cargo build`失败
**解决方案**:
1. 检查Rust工具链是否完整
2. 更新cargo: `rustup update`
3. 检查依赖版本兼容性

### 3. 前端开发服务器无法启动
**问题**: `npm run dev`失败
**解决方案**:
1. 检查Node.js版本（推荐v18+）
2. 清理npm缓存: `npm cache clean --force`
3. 重新安装依赖: `rm -rf node_modules && npm install`

### 4. Tauri窗口无法显示
**问题**: `tauri dev`后窗口不显示
**解决方案**:
1. 检查前端开发服务器是否运行
2. 检查Vite配置是否正确
3. 查看控制台错误信息

## 六、开发规范

### 1. 代码规范
- **Rust**: 遵循Rust官方代码规范
- **TypeScript**: 使用ESLint和Prettier
- **Git**: 使用Conventional Commits规范

### 2. 项目结构
```
smart-translator/
├── src-tauri/              # Rust后端
│   ├── src/               # Rust源代码
│   ├── Cargo.toml         # Rust依赖配置
│   └── tauri.conf.json    # Tauri配置
├── src/                    # React前端
│   ├── components/        # React组件
│   ├── styles/            # 样式文件
│   └── App.tsx            # 主应用组件
├── public/                 # 静态资源
├── package.json           # Node.js依赖
└── README.md              # 项目说明
```

### 3. 版本控制
- 使用Git进行版本控制
- 创建`main`分支作为稳定版本
- 使用`feature/*`分支开发新功能
- 使用`bugfix/*`分支修复bug

## 七、下一步行动

### 1. 完成环境设置
按照本指南完成所有开发工具的安装。

### 2. 初始化项目
使用Tauri CLI初始化项目，并验证开发环境。

### 3. 开始开发
按照开发计划开始第一阶段开发。

---
*指南创建时间: 2024年12月19日*
*版本: v1.0*